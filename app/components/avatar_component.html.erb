<span class="grid flex-none place-items-center rounded-full font-display font-bold text-white <%= background_color_class %> <%= size_class %>"
      title="<%= @user.full_name %>">
  <%= @user.initials %>
</span>

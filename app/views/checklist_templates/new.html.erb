<% content_for :title, "Nová šablona checklistu — Dílna" %>
<% content_for :breadcrumb, "Šablony checklistů" %>
<% content_for :page_title, "Nová šablona checklistu" %>

<div class="max-w-[640px]">
  <%= render CardComponent.new do %>
    <%= render "form", checklist_template: @checklist_template %>
  <% end %>
</div>

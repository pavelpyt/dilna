<% content_for :title, "Nový člen týmu — Dílna" %>
<% content_for :breadcrumb, "Tým" %>
<% content_for :page_title, "Nový člen týmu" %>

<div class="max-w-[640px]">
  <%= render CardComponent.new do %>
    <%= render "form", user: @user %>
  <% end %>
</div>

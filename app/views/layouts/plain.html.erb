<!DOCTYPE html>
<html lang="cs">
  <head>
    <%= render "shared/document_head" %>
  </head>

  <body>
    <div class="flex min-h-screen items-center justify-center px-5 py-12">
      <div class="w-full max-w-[420px]">
        <div class="mb-7 flex items-center justify-center gap-2.5">
          <div class="grid size-[30px] place-items-center rounded-md bg-live font-display text-lg font-extrabold text-ink">D</div>
          <div class="font-display text-[19px] font-extrabold tracking-[0.02em]">Dílna</div>
        </div>

        <%= render "shared/flash_messages" %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>

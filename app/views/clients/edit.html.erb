<% content_for :title, "#{@client.name} — úprava — Dílna" %>
<% content_for :breadcrumb, "Klienti" %>
<% content_for :page_title, "Úprava klienta" %>

<div class="max-w-[640px]">
  <%= render CardComponent.new do %>
    <%= render "form", client: @client %>
  <% end %>
</div>

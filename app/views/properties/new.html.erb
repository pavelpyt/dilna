<% content_for :title, "Nové místo — Dílna" %>
<% content_for :breadcrumb, @client.name %>
<% content_for :page_title, "Nové místo" %>

<div class="max-w-[640px]">
  <%= render CardComponent.new do %>
    <%= render "form", client: @client, property: @property %>
  <% end %>
</div>
